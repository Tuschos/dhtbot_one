# dhtbot_one
This repository contains the ROS2 packages for controlling and simulating a **differential drive mobile robot** using **ROS2 Humble**.  
The project includes the robot description, hardware interface implementation, and integration with an STM32-based controller.
